# 🤖 LLM Чат - Веб-приложение для общения с открытыми LLM моделями

Современное веб-приложение для общения с бесплатными и открытыми языковыми моделями (LLM) через Hugging Face API.

## ✨ Возможности

- 💬 Интерактивный чат с AI моделями
- 🎨 Современный и отзывчивый дизайн
- 🔄 Выбор из нескольких популярных открытых моделей:
  - Mistral 7B (рекомендуется)
  - Llama 2 7B
  - Falcon 7B
  - Phi-2
  - Gemma 7B
- 📱 Адаптивный интерфейс для мобильных устройств
- 🚀 Простая установка и использование
- 🆓 Полностью бесплатное решение

## 🛠️ Технологии

- **Backend**: Flask (Python)
- **Frontend**: HTML, CSS, JavaScript
- **API**: Hugging Face Inference API
- **LLM модели**: Открытые модели с Hugging Face

## 📋 Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)
- Интернет-соединение для доступа к Hugging Face API

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd <project-directory>
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения (опционально)

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваш API ключ Hugging Face (опционально, но рекомендуется для лучшей производительности):

```
HUGGINGFACE_API_KEY=ваш_ключ_здесь
```

**Как получить бесплатный API ключ:**
1. Зарегистрируйтесь на [Hugging Face](https://huggingface.co/join)
2. Перейдите в [Settings → Access Tokens](https://huggingface.co/settings/tokens)
3. Создайте новый токен с правами на чтение
4. Скопируйте токен в файл `.env`

> **Примечание**: Приложение работает и без API ключа, но с ограничениями по количеству запросов.

## ▶️ Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## 📖 Использование

1. Откройте браузер и перейдите по адресу `http://localhost:5000`
2. Выберите желаемую LLM модель из выпадающего списка
3. Введите ваше сообщение в текстовое поле
4. Нажмите "Отправить" или клавишу Enter
5. Дождитесь ответа от AI модели

### Горячие клавиши

- **Enter** - отправить сообщение
- **Shift + Enter** - новая строка в сообщении

## 🏗️ Структура проекта

```
.
├── app.py                 # Flask backend приложение
├── requirements.txt       # Зависимости Python
├── .env.example          # Пример файла с переменными окружения
├── .gitignore           # Игнорируемые файлы для Git
├── README.md            # Документация
├── static/
│   ├── style.css        # Стили интерфейса
│   └── script.js        # JavaScript логика
└── templates/
    └── index.html       # HTML шаблон главной страницы
```

## 🔧 API Endpoints

### POST /api/chat
Отправка сообщения в LLM модель

**Запрос:**
```json
{
  "message": "Привет, как дела?",
  "model": "mistral"
}
```

**Ответ:**
```json
{
  "success": true,
  "response": "Ответ от модели",
  "model": "mistralai/Mistral-7B-Instruct-v0.2"
}
```

### GET /api/models
Получение списка доступных моделей

**Ответ:**
```json
{
  "models": {
    "mistral": "mistralai/Mistral-7B-Instruct-v0.2",
    "llama": "meta-llama/Llama-2-7b-chat-hf",
    ...
  }
}
```

## ⚙️ Настройка

### Изменение порта

По умолчанию приложение запускается на порту 5000. Чтобы изменить порт, отредактируйте последнюю строку в `app.py`:

```python
app.run(debug=True, host='0.0.0.0', port=5000)  # Измените 5000 на нужный порт
```

### Добавление новых моделей

Чтобы добавить новую модель, отредактируйте словарь `AVAILABLE_MODELS` в `app.py`:

```python
AVAILABLE_MODELS = {
    "mistral": "mistralai/Mistral-7B-Instruct-v0.2",
    "your_model": "organization/model-name",  # Добавьте здесь
    ...
}
```

И обновите список в `templates/index.html`:

```html
<option value="your_model">Your Model Name</option>
```

## 🐛 Решение проблем

### Модель долго отвечает
- Первый запрос к модели может занять 20-30 секунд, так как модель загружается
- Последующие запросы обрабатываются быстрее
- Используйте API ключ для приоритетного доступа

### Ошибка "Model is currently loading"
- Подождите 30-60 секунд и попробуйте снова
- Модели на бесплатных серверах Hugging Face загружаются по требованию

### Ограничения API
- Без API ключа есть ограничение на количество запросов в час
- Получите бесплатный API ключ для увеличения лимитов

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

Contributions, issues и feature requests приветствуются!

## 👨‍💻 Автор

Создано с помощью Claude AI

## 🔗 Полезные ссылки

- [Hugging Face](https://huggingface.co/)
- [Flask Documentation](https://flask.palletsprojects.com/)
- [Hugging Face Inference API](https://huggingface.co/docs/api-inference/index)

---

**Наслаждайтесь общением с AI!** 🚀
